#pragma once
#ifndef LIBGUI_UTILS_HPP
#define LIBGUI_UTILS_HPP

#include <Types.hpp>

namespace GUI
{
    namespace utils
    {
        Pos &defaultConverter(Pos &);
    } // namespace utils

} // namespace GUI

#endif